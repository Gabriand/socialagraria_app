import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Colores Principales
  static const Color accent = Color(0xFFA3B18A);
  static const Color primary = Color(0xFF588157);
  static const Color primaryDarker = Color(0xFF3A5A40);
  static const Color primaryDarkest = Color(0xFF344E41);

  // Colores de Estado
  static const Color success = Color(0xFF4CAF50); // Verde para un "Match"
  static const Color error = Color(0xFFF44336); // Rojo para un "No"

  // Colores Neutrales
  static const Color background = Color(0xFFDAD7CD);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Colores opcionales
  static const Color transparent = Color(0x732B2B2B);
}
